'use strict';

const {
  Utils,
} = require(`./utils`);

const {
  generateCategory,
  generateDescription,
  generatePictureName,
  generateTitle,
  generateType,
  generateSum
} = require(`./generateOffers`);

module.exports = {
  Utils,
  generateCategory,
  generateDescription,
  generatePictureName,
  generateTitle,
  generateType,
  generateSum
};
